<UserControl x:Class="Cm93.UI.Modules.SelectTeamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="4*"/>
			<ColumnDefinition Width="6*"/>
		</Grid.ColumnDefinitions>
		
		<Grid Grid.Column="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="1*"/>
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="ExtraBold" Foreground="White" Text="Select your team" />
			<ComboBox Grid.Row="1" FontSize="18"  Margin="15" Height="40" VerticalAlignment="Center" HorizontalAlignment="Left" FontWeight="ExtraBold" Foreground="White" ItemsSource="{Binding AvailableTeams}" SelectedItem="{Binding SelectedTeam}" />
			<Button Grid.Row="2" Margin="10" Height="40" Width="65" Content="Start" VerticalAlignment="Bottom" HorizontalAlignment="Right" cal:Message.Attach="Start" />
		</Grid>

		<Grid Grid.Column="1">
			
		</Grid>
	</Grid>
</UserControl>
